import type { AuthCredentials } from '@/types';

/**
 * Placeholder for real authentication; wire to your API or auth provider.
 */
export async function signInWithCredentials(
  _credentials: AuthCredentials,
): Promise<void> {
  await Promise.resolve();
}
